[← Back](../../../README-ja.md) | [English](README.md) | [Japanese](README-ja.md)

# ExBuchholz

拡張ブーフホルツ psi。Maksudov による Buchholz の崩壊関数の拡張で、`ψ` の添字が
`ω` 以下の数ではなく、この系自身が名前を付けられる任意の順序数になる。

```
C_v^0(a)     = {b | b < Ω_v}
C_v^(n+1)(a) = {b + c, ψ_u(e) | u, b, c, e ∈ C_v^n(a) ∧ e < a}
C_v(a)       = ⋃_{n<ω} C_v^n(a)
ψ_v(a)       = min {g | g ∉ C_v(a)}
```

`Ω_0 = 1`、`v > 0` では `Ω_v` は濃度 `ℵ_v` の最小の順序数。可算な極限は `p0(Λ)`
で、`Λ` は最初の omega 不動点である。

## 項の型

再帰引数 3 つの単一の帰納型。相互帰納も入れ子帰納も使わないので、以下の再帰は
全部構造帰納である。

```lean
inductive Term where
  | nil  : Term                       -- 0
  | cons : Term → Term → Term → Term  -- ψ_a(b) + t
```

項は主項 `ψ_a(b)` の有限和である。添字が項であることが、この拡張の中身そのものである。

## ファイル

| ファイル | 中身 |
|---|---|
| `Basic.lean` | `Term`、`cmp`、`lt`、`le`、決定可能性、`cmp_self`、`cmp_swap`、`cmp_eq_iff` |
| `Order.lean` | `lt_irrefl`、`lt_trans`、`lt_trichotomy`、`lt_asymm` |
| `Std.lean` | `G`、`isOT`、`OT`、決定可能性 |
| `WF.lean` | `not_wellFounded_lt`、`cmp_cons_cons'`、`OT_head`、`OT_tail`、`OT_tail_head_le`、`OTLt`、`acc_nil` |
| `Sum.lean` | `leadCount`、`dropLead`、`lead_lex`、`acc_of_headLe`、`acc_of_OT`、`wellFounded_OTLt` |
| `Ord.lean` | 順序数側。`Omega`、`Clos`、`CSet`、`psi`、濃度評価、`psi_lt_Omega_succ`、`psi_notMem`、`Omega_le_psi`、`psi_mono`（mathlib が要る） |
| `Eval.lean` | `Term.val`。順序数への評価。`Lam` と `val_lt_Lam`（mathlib が要る） |
| `Mono.lean` | `val_lt_val`、`val_mem_CSet_arg`、`valHom`、`OTLt_wf`（mathlib が要る） |
| `FS.lean` | `dom`、`fs`（基本列 `X[Y]`）、`fs_lt`（降下）、Buchholz 3.2(b)、場合 4 の塔、展開系 `exb` |
| `Closure.lean` | 連結、`G°`、`⊲`、Buchholz 3.4、3.5、3.6 |

## 順序

`cmp` は主項のリストの辞書式順序で、真の前半が小さい方になる。主項どうしは
`(添字, 引数)` の辞書式で比べる。Buchholz の `(<1)`–`(<3)` で、添字を数から項に
一般化したものである。

`Order.lean` はこれが狭義の線形順序であることを証明する。この 3 つは
`Googology.Core.Morphism` の `OrdHom.injective` が要求するものそのものなので、
この系への翻訳は順序を保つだけで単射になる。

## 標準形

`ψ_a(b)` が標準形であるとは、`ψ_a` がまだ届く `b` の部分項が全部 `b` より真に
小さいことをいう。和が標準形であるとは、主項が広義単調減少であることをいう。
集める関数 `G` は Buchholz のものだが、内側の `ψ_c(d)` の添字 `c` 自身が項なので、
`d` と一緒に `c` も集める点が違う。

`isOT` は `Bool` を返す決定手続きなので、小さい場合は `#guard` が計算で確かめる。
`Std.lean` に入れたものには、`ε₀ = ψ_0(Ω)` が標準形であること、`1 + ω` は主項が
増えるので標準形でないこと、`ψ_0(ψ_0(Ω))` は `Ω` が引数より下にないので標準形で
ないことが含まれる。

## なぜ標準形が要るか

`WF.lean` は、**全部の項の上の順序が整礎でない**ことを証明している。

```
Ω > ψ_0(Ω) > ψ_0(ψ_0(Ω)) > ψ_0(ψ_0(ψ_0(Ω))) > ⋯
```

`cmp` が添字を先に比べ、`0 < ψ_0(0)` なので、これが無限に下がる。この列は 3 項目で
`OT` の外に出る。だから `not_wellFounded_lt` は欠陥ではなく、標準形の条件が存在する
理由そのものである。

## 較正

出典は、巨大数研究 Wiki の P進大好きbot 氏による「拡張 Buchholz OCF に伴う順序数
表記」の記事である。以下は全部、その条項と 1 つずつ突き合わせた。

| | 出典 | ここ | 判定 |
|---|---|---|---|
| 項 | `()`、`⟨X₁,X₂⟩`、`(X₁,…,X_m)` | `nil`、`cons a b nil`、`cons …` | 同型 |
| 順序 `<` | 6 条項 | `cmp` | 全条項で一致 |
| 評価 `o` | `o⟨Y₁,Y₂⟩ = ψ_{o Y₁}(o Y₂)`、和は和 | `Term.val` | 同一 |
| `G(X,Y) ◁ Z` | `X ≤ W₁` のとき `W₂ < Z` と、`W₁`・`W₂` への再帰 | `G` | **訂正した**。下記 |
| `OT` | `⟨X₁,X₂⟩ ∈ OT ↔ X₁, X₂ ∈ OT ∧ G(X₁,X₂) ◁ X₂`、和は広義単調減少 | `isOT` | 一致 |
| 基本列 | `dom` と `X[Y]` | 両方済 | 転記済み。ω・Ω・ω^ω・ε₀ で検算 |

### 訂正の中身

`G` は当初、内側の `ψ_c(d)` について引数 `d` と一緒に添字 `c` も集めており、
`isOT` が出典より狭義に強くなっていた。出典は `d` だけを集め、`c` には再帰で
しか届かない。`G` をそれに合わせた。

訂正後、このディレクトリの定理は**全部そのまま通った**。証明が余分な条件に
寄りかかっていなかった、ということである。

### まだ開いていること

出典は、`o` の `OT` への制限が `C_0(Λ)` への順序**同型**だと述べている。単調で
単射という半分はここの定理である（`val_lt_val`、`val_inj_of_OT`）。全射性は
証明していない。

## 整礎性：済んだところと残り

`Sum.lean` が次を証明した。

```lean
theorem wellFounded_OTLt (HP : ∀ a b, OT (ψ_a(b)) → Acc OTLt (ψ_a(b))) :
    WellFounded OTLt
```

したがって問題は**主項についての仮定 1 つ**に落ちた。和の側に内容はもう無い。

その半分の筋道。標準形は主項が広義単調減少である（`OT_tail_head_le`）ので、頭が
`p` 以下の標準形は「`p` の連続する塊」＋「頭が `p` より真に小さい項」の形になる。
`lead_lex` は、比較がこの表示を辞書式に読むことを言う。塊が長い方が大きく、塊が
同じ長さのときだけ残りが効く。だから帰納は `Nat` の塊の長さで回り、長さを固定した
中では塊のあとの部分の可到達性で回る。`OT` の外の項は自動的に可到達である。
`OTLt y t` が `OT t` を要求するからである。

`acc_of_headLe` は全体仮定をまったく必要としない。境界の項の可到達性そのものが
帰納の軸になっている。

**残っているのは `HP` 自身**である。

```
∀ a b, OT (ψ_a(b)) → Acc OTLt (ψ_a(b))
```

これが崩壊の議論である。ここで取る道は順序数の側である。`Ord.lean` が Maksudov の
条項から順序数の上の `ψ` を定義しており、予定は評価写像 `Term → Ordinal` を作って、
`OT` の上での単調性から `OrdHom.wf` で整礎性を引き戻すことである。

`Ord.lean` には濃度評価が入った。閉包を有限段の合併として表し、各段が濃度
`max #(Iio Ω_v) ℵ₀` 以下であることを濃度の計算で示し、その最大値がどの `v` でも
`ℵ_v` であることを示した——`Ω_0 = 1` の `v = 0` でも閉包は可算である。
`ℵ_v < ℵ_{v+1}` だから、閉包は `Ω_{v+1}` 未満の順序数を覆いきれない。したがって
`ψ_v(a) < Ω_{v+1}` である。

`Eval.lean` が `Term.val` を定義した。残っているのはその `OT` 上での単調性である。
すぐに出ない理由は記録しておく価値がある。`ψ_v` の引数についての弱い単調性では
足りない。`ψ_v(a) = ψ_v(a+1)` は実際に起きる——`a` が `C_v(a)` の中に届かないとき
ちょうどそうなる。狭義にするのが標準形の条件の役目である。4 段階は `Eval.lean` の
冒頭に書いてある。構文的な道（Buchholz の集合 `W_u` と Bachmann 性質）を
取らなかったのは、拡張版の基本列が先に要るのに、確認できた出典が手元に無かったから
である。

### 最後の補題への道筋

Buchholz は補題 3.3 を `b ⊲_z a` という関係を経由して証明する。`b` が `a` より
下で、`G` が `b` に見るものが、その間のどの `c` に見るものと `z` とで抑えられる、
という関係である。

| | 主張 | 状態 |
|---|---|---|
| 3.4 | `b ⊲_z a`、`G_u a < a`、`G_u z < b` ⟹ `G_u b < b` | **済** |
| 3.5 | `b₀ ⊲_z b` ⟹ `a + b₀ ⊲_z a + b`、`ψ_u(b₀) ⊲_z ψ_u(b)`、`ψ_{b₀}(0) ⊲_z ψ_b(0)` | **済** |
| 3.2(b) | 項で添字づけられた定義域で `z₁ < z₂` ⟹ `a[z₁] < a[z₂]` | **済**（`fs_mono`） |
| 3.6 | `z ∈ dom a` ⟹ `a[z] ⊲_z a` | **済**。ただし `SubBound` を仮定 |
| 3.3 | `a, z ∈ OT`、`z ∈ dom a` ⟹ `a[z] ∈ OT` | 未 |

仕事をするのは 3.4 である。「`z` に相対して抑えられる」を、標準形の条件そのものに
変える。`Closure.lean` には 3.4 と、3.5 の 3 つの形と、3.6（`Trian_fs`）がある。
3.5 の各形は「2 つの項の狭義の間にある項の形」を決める分解補題に乗っている。

`Trian_fs` が 3.6 である。証明は `size` についての帰納法で、`fs` の枝ごとに
場合を分ける。7 つの枝のうち 6 つは 3.5 と、小さな補題 `Trian_nil`、
`Trian_self`、`Trian.of_nil`、`Trian.repeatPrin` で閉じる。残る 1 つが
Buchholz の場合 4 で、`dom X₂ = ψ_Z(0)` が `ψ_{X₁}(X₂)` より下になく、指標を
`Z` から組み直す枝である。数字 `n` で展開すると、次の塔を回ることになる。

```
W₀ = ψ_{Z[0]}(0)        W_{i+1} = ψ_{Z[0]}(X₂[W_i])
(ψ_{X₁}(X₂))[n̲] = ψ_{X₁}(X₂[W_n])
```

この塔は `FS.lean` に `tower` として定義してある。枝との一致が `fs_numeral`、
登ることが `tower_lt` と `tower_val_lt`、各段が `X₂` の指標として使えることが
`tower_lt_dom` である。登るところに 3.2(b)、すなわち `fs` の指標についての
単調性が要る。それが同じファイルの `fs_mono` である。

その上で `Trian_case4` が、塔の添字 `Z[0]` だけについての 1 つの主張から場合 4
を出す。これを `SubBound` と呼ぶ。

```
X₂[W₀] ≤ c ≤ X₂  ⟹  G_u(Z[0]) ≼ {c} ∪ G_u(c) ∪ {0}
```

`tower_G_le` がこれを塔の上まで運ぶ。同じ評価が各段 `W_i` について、`X₂[W_i]`
と `X₂` の間のどの `c` に対しても成り立つ。帰納法は段について回し、`X₂` での
3.6 と塔の単調性を使う。

この評価は `c` に相対でなければならない。Buchholz 自身の不変量は絶対形
`G_u(W_i) < X₂[W_i]` である。彼の体系では添字が数なので `G` が添字の中に入らず、
それで済む。拡張版では添字が項なので、絶対形は偽になる。`A = ψ_0(ψ_Ω(0))`、
`X = ψ_Ω(ψ_{A+1}(0))` とすると、塔の第 0 段は `ψ_A(0)` で、その段の生む値も
`ψ_A(0)` だが、`G_0` はそこに `ψ_Ω(0)` を見る。`A` は可算なので
`ψ_Ω(0) > ψ_A(0)` である。

3.6 が 3.3 を呼ぶ箇所は、もう `SubBound` だけである。`Z[0]` についての主張で、
`X₂` が標準形であることから出さなければならない。Buchholz は 3.3 と 3.6 を 1 つ
の同時帰納法で証明しており、ここのように分けると、これが残る。上の項と
`SubBound` の確認は `test/ExBuchholzCheck.lean` にある。サイズ 7 以下の標準形で
場合 4 の形 158 個（可算とは限らない）について、サイズ 2 以下の全レベル、サイズ
4 以下の全候補 `c` で確認してある。サイズ 8（651 個）でも同じ結果である。

3.3 は 3.4 と 3.6 を組み合わせる。

## 状態

| | |
|---|---|
| 項の型、順序、決定可能性 | 済 |
| 狭義線形順序 | 済 |
| `G`、標準形、決定可能性 | 済 |
| 制限しない順序が整礎で**ない**こと | 済 |
| 順序が辞書式の積であること、標準形が部分に遺伝すること | 済 |
| 和：主項の可到達性から `WellFounded OTLt` が出ること | 済 |
| 主項の可到達性 | **未**。下を参照 |
| 基本列、`Rewrite` の値 | **未** |
| 順序数の上の `ψ` の定義と基本性質 | 済 |
| `C_v(a)` の濃度評価、したがって `ψ_v(a) < Ω_{v+1}` | 済 |
| `ψ ∉ C_v(a)`、`Ω_v ≤ ψ_v(a)`、引数についての単調性 | 済 |
| 閉包が下に閉じていること、`ψ_v(a)` が加法的主要であること | 済 |
| 評価写像 `Term → Ordinal` | 定義済 |
| 項の値が `v ↦ ω_v` の不動点にならないこと | 済 |
| その `OT` 上での単調性 | 済 |
| **`OTLt` の整礎性。仮定なし** | **済**（`OTLt_wf`） |
| 標準形が整列すること、`val` が単射であること | 済 |
| `dom` と基本列 `X[Y]` | 済（`FS.lean`） |
| 展開系 `exb` | 定義済 |
| **基本列が降下すること**：`Y < dom X → X[Y] < X` | **済**（`fs_lt`） |
| `Ω` 未満で、`0` 以外の標準形は後続か `ω` 極限 | 済（`dom_eq_one_or_tw`） |
| **一歩の展開が可算標準形を狭義に下げる** | **済**（`step_lt`） |
| `G` が添字について反単調であること、閉包の和の枝 | 済（`G_subset_of_le`、`OT_cons_fs`） |
| `⊲` についての Buchholz 3.4 と 3.5 | 済（`Closure.lean`） |
| Buchholz 3.2(b)：`fs` が指標について単調 | 済（`fs_mono`） |
| 場合 4 の塔と `(ψ_{X₁}(X₂))[n̲] = ψ_{X₁}(X₂[W_n])` | 済（`tower`、`fs_numeral`） |
| Buchholz 3.6：`z ∈ dom a → a[z] ⊲_z a` | 済。ただし `SubBound` を仮定（`Trian_fs`） |
| `SubBound` そのもの | **未証明**。場合 4 の形 158 個について計算で確認 |
| `OT` と `· < Ω` が展開で保たれること | **未証明**。最後の穴。`test/ExBuchholzCheck.lean` で計算により確認 |

例外的に `sorry` を許しているわけではない。ファイルに `sorry` も `axiom` も無い。

### 出典のあるもの・無いもの

上の `ψ` の定義は Maksudov のもので、Googology Wiki に載っている。それに対応する
表記系は P進大好きbot によるもので、ここの項・順序・`G`・`OT`・評価はその記事に
従っている。条項ごとの突き合わせは、上の**較正**の節にまとめてある。同記事の基本列はまだ実装
していない。

## 名前について

`EBuchholz` でも `EBP` でもなく `ExBuchholz` にしました。mathlib は「拡張した X」を
`E` の接頭辞で綴りる——`ENat`、`EReal`、`ENNReal`、`EMetricSpace`——が、その読み方は
人名の前では壊れる。`EBuchholz` はイニシャル「E. Buchholz」と見分けがつかない。
`Ex` なら「拡張」以外には読めない。
よければ 1 行の変更で済む。

## 出典

- Extended Buchholz's function, Googology Wiki.
  https://googology.miraheze.org/wiki/Extended_Buchholz%27s_function
- W. Buchholz, A new system of proof-theoretic ordinal functions,
  Annals of Pure and Applied Logic 32 (1986) 195–207.
