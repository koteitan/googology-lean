[← Back](README-ja.md) | [English](spec.md) | [Japanese](spec-ja.md)

# 仕様

このライブラリが何で、どう並んでいて、どういう規約で書かれているかをまとめます。
`README.md` は使う方の入口で、この文書は手を入れる方のためのものです。

## 1. 扱う範囲

このライブラリが扱うのは**展開系**です。状態の型、自然数で添字づけられた一歩、
そして停止状態の集合からなります。答える問いは、どの展開列も停止状態に着くか
どうかです。

ここには 2 種類のものが入りますが、同じ種類のものなので分けていません。

* 巨大数の側で定義された系——BMS、DBMS、Y 数列
* 証明論の文献から来た表記系——拡張ブーフホルツ ψ、Rathjen の `T(M)`

どちらも「項の型と順序と、たいていは基本列」です。違うのは役割だけで、一方は
停止性を証明したい対象、もう一方はそれを測る物差しです。

## 2. 構成

```
Googology/
  Core/            展開系、標準形、翻訳
  Rank.lean        整礎な系は順序数の測度を持ちます
  Notation/<名前>/ 系ごとに 1 ディレクトリ
  Trans/<A>/<B>.lean  翻訳。対ごとに 1 ファイル
test/              有限の検査。定理ではありません
```

### 層の規則

1. `Core/` は core Lean の外を何も import しません。mathlib 非依存の
   プロジェクトから使えます。
2. `Notation/X/` は `Notation/Y/` を import しません。系は自分自身と `Core` しか
   知りません。
3. `Trans/` だけが 2 つの系を同時に import します。
4. 対 `{X, Y}` にはファイルを **1 つ**だけ置きます。名前順で先に来る方の下に置き、
   両方向と、あれば `Equiv` をまとめて入れます。索引は `Trans/README-ja.md` に
   あります。
5. mathlib は系ごとに import します。順序数へ評価する系だけが import し、`Core`
   は決して import しません。

規則 2 が import の向きを保ちます。翻訳を `Notation/BMS/` に置くと、BMS を
import した人に翻訳先の系が全部付いてきてしまいます。

## 3. 系が出すもの

`Rewrite` の 3 つのフィールドと、停止性のための測度 1 つです。測度の行き先は
整礎関係の入った型なら何でもかまいません。残りは定理として受け取れます。すでに
別のところで停止性が証明されている系なら、フィールドと `Subrelation.wf` を出す
だけで繋がります。`Notation/BMS` がその実例です。

## 4. 何を証拠と呼ぶか

* `Googology/` の中の主張は、`sorry` が無く、公理も `propext`・
  `Classical.choice`・`Quot.sound` を超えない Lean の定理です。
* `#guard` の行は有限個の場合についての計算です。証拠ではありますが証明では
  ないので、見た目で分けます。網羅的な検査は `test/` に、定義の健全性の確認は
  その定義のそばに置きます。
* 出典から転記した定義には、そう書いて出典を挙げます。条項ごとに突き合わせて
  いない場合は、そのことも書きます。

## 5. 名前

* Lean のモジュールと、それを含むディレクトリは UpperCamelCase です。頭字語は
  大文字のままにします（`BMS`、`ExBuchholz`）。Lean のモジュールでない
  ディレクトリは小文字です（`test`）。
* 「拡張した X」は `EX` ではなく `ExX` にします。`EBuchholz` はイニシャルに
  見えてしまうからです。
* `lean_lib` の名前とその根ファイル名は完全に一致させます。

## 憲章

以下はコードではなく文書についての規則です。

### C1. README は読み手のもので、書き手のものではありません

`README.md` は、このライブラリを使いたい方に向けて書きます。何が手に入るか、
どうやってプロジェクトに足すか、何が証明されているか、から始めます。計画や
進捗や残作業から始めることはしません。

作る側の連絡——計画、現状、未解決の問い、設計の理由——は**一番下**の目立たない
見出しの下か、`spec.md` と `plan.md` に置きます。読み手はその線の手前で読むのを
やめても、必要なものを読み落とさないようにします。

### C2. どのディレクトリにも README を、両方の言語で

英語の `README.md` と日本語の `README-ja.md` を置き、1 行目に言語の切り替えと
戻るリンクを入れます。内容は同じにします。

### C3. 日本語はていねいに

日本語の文書は「です・ます」で書きます。読み手への命令形は使いません。「見よ」
ではなく「ご覧ください」、「〜すること」ではなく「〜してください」とします。

### C4. 証明していないことは、していないと書きます

結果の表では、どの行が定理でどの行がそうでないかを書きます。計算でしか
確かめていないものはそう書き、検査の規模も添えます。済んでいないものを済んだ
ように書くことはしません。

### C5. コミットメッセージ

`vA.B.C/短い要約` の後に空行を置き、変更ごとに 1 項目ずつ書きます。項目は
MECE にします。重なりを作らず、同じ変更を言い換えて 2 回書きません。
`lakefile.toml` のパッチ版数は同じコミットで上げます。

### C6. リポジトリの外のパスは書きません

ここで公開する文書が他の仕事に触れるときは、公開 URL で指します。書いた
機械の上のパスでは指しません。
